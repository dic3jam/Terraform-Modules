# basic AWS EC2 Module

Builds a terraform module of a basic AWS EC2 instances that will participate in a Docker Swarm.

The kms key for EBS encryption needs to be configured manually prior to deployment.